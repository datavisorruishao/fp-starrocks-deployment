apiVersion: v1
kind: ServiceAccount
metadata:
  name: iceberg-rest-catalog
  namespace: duckdb
  labels:
    app.kubernetes.io/name: iceberg-rest-catalog
    app.kubernetes.io/managed-by: manual
---
apiVersion: v1
kind: Service
metadata:
  name: iceberg-rest-catalog
  namespace: duckdb
  labels:
    app.kubernetes.io/name: iceberg-rest-catalog
    app.kubernetes.io/managed-by: manual
spec:
  type: ClusterIP
  ports:
    - name: rest
      port: 8181
      targetPort: rest
      protocol: TCP
  selector:
    app.kubernetes.io/name: iceberg-rest-catalog
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: iceberg-rest-catalog
  namespace: duckdb
  labels:
    app.kubernetes.io/name: iceberg-rest-catalog
    app.kubernetes.io/managed-by: manual
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: iceberg-rest-catalog
  template:
    metadata:
      labels:
        app.kubernetes.io/name: iceberg-rest-catalog
    spec:
      serviceAccountName: iceberg-rest-catalog
      initContainers:
        - name: mysql-jdbc
          image: curlimages/curl:8.7.1
          imagePullPolicy: IfNotPresent
          command:
            - sh
            - -c
            - |
              set -euo pipefail
              curl -fsSL \
                https://repo1.maven.org/maven2/com/mysql/mysql-connector-j/8.0.33/mysql-connector-j-8.0.33.jar \
                -o /work/mysql-connector-j-8.0.33.jar
          volumeMounts:
            - name: mysql-jdbc
              mountPath: /work
      containers:
        - name: iceberg-rest
          image: tabulario/iceberg-rest:1.6.0
          imagePullPolicy: IfNotPresent
          command:
            - java
            - -cp
            - /usr/lib/iceberg-rest/iceberg-rest-image-all.jar:/usr/lib/iceberg-rest/mysql-connector-j-8.0.33.jar
            - org.apache.iceberg.rest.RESTCatalogServer
          ports:
            - name: rest
              containerPort: 8181
              protocol: TCP
          env:
            - name: CATALOG_WAREHOUSE
              value: "s3://datavisor-dev-us-west-2-iceberg/cre-6630/duckdb/"
            - name: CATALOG_IO__IMPL
              value: "org.apache.iceberg.aws.s3.S3FileIO"
            - name: CATALOG_URI
              value: "jdbc:mysql://iceberg-catalog-mysql.duckdb:3306/iceberg_catalog"
            - name: CATALOG_JDBC_USER
              valueFrom:
                secretKeyRef:
                  name: iceberg-rest-catalog-secret
                  key: jdbc-user
            - name: CATALOG_JDBC_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: iceberg-rest-catalog-secret
                  key: jdbc-password
            - name: AWS_REGION
              value: "us-west-2"
          readinessProbe:
            tcpSocket:
              port: rest
            initialDelaySeconds: 10
            periodSeconds: 10
            timeoutSeconds: 3
          livenessProbe:
            tcpSocket:
              port: rest
            initialDelaySeconds: 20
            periodSeconds: 20
            timeoutSeconds: 3
          resources:
            requests:
              cpu: 250m
              memory: 512Mi
            limits:
              cpu: "1"
              memory: 1Gi
          volumeMounts:
            - name: mysql-jdbc
              mountPath: /usr/lib/iceberg-rest/mysql-connector-j-8.0.33.jar
              subPath: mysql-connector-j-8.0.33.jar
      volumes:
        - name: mysql-jdbc
          emptyDir: {}
